<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:gravity="center"
    android:orientation="vertical"
    tools:context=".FindPartner">

    <ImageView
        android:id="@+id/qrpartnerImg"
        android:layout_width="150dp"
        android:layout_height="160dp"
        android:layout_marginBottom="10dp"
        />

    <TextView
        android:id="@+id/qrpartner_name"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textAlignment="center"
        android:textColor="@color/colorDark"
        android:textSize="24sp" />

    <TextView
        android:id="@+id/qr_already_partner"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="You already add this partner"
        android:textAlignment="center"
        android:textColor="@color/colorDark"
        android:textSize="12sp" />

    <Button
        android:id="@+id/qrinsertPartner"
        android:layout_width="150dp"
        android:layout_height="wrap_content"
        android:padding="10dp"
        android:text="Add Partner"
        android:background="@drawable/ok_button"
        />

    <Button
        android:id="@+id/cAddQRBtn"
        android:layout_marginTop="5dp"
        android:layout_width="150dp"
        android:layout_height="wrap_content"
        android:background="@drawable/test_button"
        android:text="Cancel" />

</LinearLayout>